[defaults]
inventory = ./inventory.ini
private_key = ~/.ssh/ansible_key
remote_user = ubuntu
ask_pass = false
callback_whitelist = log, timer

[privilege_escalation]
become = True
become_method = sudo
become_user = root
become_ask_pass = false

[ssh_connection]
pipelining = True